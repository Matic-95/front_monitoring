import React from 'react'
import ReactDOM from 'react-dom/client'
import SSLMonitoringForm from './SSLMonitoringForm'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SSLMonitoringForm />
  </React.StrictMode>,
)
